# Production API base — คัดลอกเป็น .env.production แล้วเติมค่าจริง
# (ไฟล์นี้เป็น template ปลอดภัยสำหรับ commit; .env.production ถูก gitignore)
VITE_API_BASE=https://projectspecai-production.up.railway.app/api/v1